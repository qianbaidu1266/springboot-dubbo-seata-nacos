# 2026-09-11

## Seata 2.6.0 服务端本地部署环境梳理（Mac）

部署根目录：`/Users/yangchao/Desktop/zhongjianjian/`
- `seata-2.6.0/seata-server/`（TC，本目录即发布包根，含 bin/conf/lib/target）
- `seata-2.6.0/seata-namingserver/`（2.6.0 新增的独立控制台组件）
- `nacos-2.4.3/`（实际在跑的注册/配置中心）

### 启动方式（已验证）
```bash
export JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-17.jdk/Contents/Home
cd /Users/yangchao/Desktop/zhongjianjian/seata-2.6.0/seata-server
sh bin/seata-server.sh          # 启动（内部 nohup，输出到 bin/nohup.out）
sh bin/seata-server.sh stop     # 停止
```
手工等价命令（用户惯用）：
```bash
cd .../seata-server
$JAVA_HOME/bin/java -Dlog.home=~/logs/seata \
  -Dloader.path=$PWD/lib \
  -Dspring.config.additional-location=$PWD/conf/ \
  -Dspring.config.location=$PWD/conf/application.yml \
  -jar target/seata-server.jar
```

### 关键结论（踩坑点）
1. **`target/seata-server.jar` 只有 707KB**，是 Spring Boot PropertiesLauncher 瘦包；真正的类在 `lib/`，
   `-Dloader.path=lib` **必需**，不能省略，否则起不来。
2. **JDK 17 实测可用**（探针实例 18091 端口，405ms 启动成功，仅 prometheus 9898 端口被占用报非致命错误）。
   但官方脚本不带 `--add-opens`，JDK 17 下 CGLIB 配置代理（`FileConfiguration$$EnhancerByCGLIB`）正常。
   历史上成功运行的实例是 **JDK 1.8.0_471**（Dockerfile 也是 temurin 8）。
3. **本机默认 java 是 25，且 `JAVA_HOME` 为空** → 直接 `sh bin/seata-server.sh` 会用 JDK 25（未验证，不要用），
   必须先 `export JAVA_HOME`。
4. **2.6.0 起 console UI 从 seata-server 迁到了独立 `seata-namingserver`**，seata-server 不再提供 7091。
   启动日志原文：`you can visit seata console UI on namingserver.`
5. 端口：Nacos 8848(+/9848/9849/7848) · seata-server 8091（TC RPC）+ 9898（prometheus metrics）。
6. **实际生效的配置来自 Nacos**，不是本地 `conf/application.yml`：
   `seata.config.type=nacos` → 读取 `SEATA_GROUP/seataServer.properties`
   （内容：`store.mode=file`、`store.file.dir=sessionStore`、recovery 周期、retry timeout 等）。
   本地 application.yml 的 store 段只是兜底/示例。
7. **注册信息**：`SEATA_GROUP@@seata-server`，cluster=`default`，healthy。
   但注册 IP 是 **198.18.0.1**（不是 127.0.0.1）——因 `seata.registry.preferred-networks: 192.168.43.*`
   未匹配到本机网卡，选到了代理/VPN 网卡。若业务服务连不上 TC，优先排查这里。
8. vgroupMapping 已配齐：`business/order/storage/account-service-seata-service-group → default`，
   与 samples-* 各服务 `seata.tx-service-group` 一一对应，client 端无需改动。
9. `bin/seata-server-stop.sh` 是用户自定义脚本（grep `seata-server.jar` 后 kill -15/-9），
   官方 `sh bin/seata-server.sh stop` 用 `ps aux | grep seata-server`。

### 日志
- 启动输出：`seata-server/bin/nohup.out`（脚本启动时）
- 运行日志：`~/logs/seata/seata-server.8091.all.log` / `.error.log` / `.warn.log`
- GC：`~/logs/seata/seata_gc.log`

## 四个业务服务的启动顺序（已实测验证）

### 依赖方向（Dubbo 引用关系，来自代码）
- business → storage、order
- order → account
- storage、account → 叶子，无下游

### 端口
| 服务 | HTTP | Dubbo | 事务组 |
|---|---|---|---|
| account | 8102 | 20883 | account-service-seata-service-group |
| order | 8101 | 20880 | order-service-seata-service-group |
| storage | 8109 | 20888 | storage-service-seata-service-group |
| business | 8104 | 10001 | business-service-seata-service-group |

### 结论：顺序是强制的（默认配置下）
1. account / storage（可并行，无下游）
2. order（依赖 account）
3. business（依赖 order + storage）

原因：代码里写的是 `@DubboReference(version = "1.0.0")`，**没有 check=false**。
反射读取确认 dubbo 2.7.13 的 `@DubboReference#check` 注解默认值 = **true**。

### 验证方法（可复用）
用 `samples-order/target/samples-order-1.0.0.jar`（90MB fat jar）解出 `BOOT-INF/lib/*.jar` 作为 classpath，
写一个不依赖 Spring/MySQL 的 `ReferenceConfig` 探针指向 `nacos://127.0.0.1:8848`：
- `check=true` + 无提供者 → `IllegalStateException: Failed to check the status of the service ...AccountDubboService. No provider available for the service ...:1.0.0`（约 1253ms）
- `check=false` → 约 1164ms 正常拿到代理
放在 Spring Boot 里该异常抛在 `ReferenceBean.getObject()`，会让 context refresh 失败 → **应用直接起不来**，
不是"起来了但调用报错"。

### 想解除顺序依赖（可选，尚未落地）
- 逐处改：business 的 2 个 + order 的 1 个 `@DubboReference` 加 `check = false`
- 或全局：business/order 的 yml 加 `dubbo.consumer.check: false`
注意 check=false 只跳过启动校验，调用时提供者仍缺失依然会失败（错误推迟到调用期）。

### 环境依赖（顺序之外，优先级更高）
- **Nacos 8848 必须先起**：4 个服务都靠它做 Dubbo 注册 + Seata 注册/配置
- seata-server TC 8091：不在也能启动，但一调 `/business/dubbo/buy` 就找不到 TC
- **MySQL 3306 + 库 `seata_demo`**：建库脚本 `sql/db-seata.sql`，含 t_account / t_order / t_storage / **undo_log**（AT 模式必需）
- 当前状态（2026-09-11 00:1x）：Nacos ✅、seata-server(8091) ✅、MySQL ❌（3306 无监听）、四个业务服务 ❌ 均未启动

### 顺带发现的死配置
business / order 的 yml 里有 `dubbo.consumer.provided-by: dubbo-xxx-example`，
但 dubbo 2.7.13 的 `ConsumerConfig` / `AbstractInterfaceConfig` / `AbstractReferenceConfig` 里
**都不存在 providedBy 属性**（这是 Dubbo 3.x 才有的配置项），当前被静默忽略，属于无效配置。

## 更新 `documents/本地编译启动手册-实测跑通.md`

按"启动顺序"结论重写了第 5 节（5.1 依赖方向 / 5.2 顺序 / 5.3 原因 / 5.4 解除方式 / 5.5 命令），
并修掉三处与代码或现状不符的地方：

1. **端口表错**：account 的 Dubbo 端口文档写 20882，实际 `application.yml` 是 **20883**，已改。
2. **第 3 节代码块串位**：标题是"Nacos HTTP API 增删查"，块里却是 seata-server 的启动命令（与第 4 节重复）。
   已换成真正的 Nacos 配置读写：`script/config-center/nacos/nacos-config.sh -h -p -g` + v1 HTTP API 增删查示例。
3. **TC 注册网段已过时（重要）**：文档 4.1 让填 `preferred-networks: 192.168.43.*`，
   但 2026-09-11 复核本机 `en0 = 192.168.5.3`，`192.168.43.*` 匹配不上，
   **当前实际跑着的 TC 注册成了 `198.18.0.1:8091`**（代理虚拟网卡）。
   已在 4.1 加显式警告 + 在第 110 行附近补了确认注册 IP 的 curl 命令，踩坑表第 1 行同步更新。

踩坑表第 5 行也纠正了定性：原来写"与 order 并行启动的注册竞态"，
实际是 `@DubboReference` 默认 `check=true` 导致的**必然**启动失败，不是偶发竞态。

## 【已修复】TC 注册 198.18.0.1 → 导致 account/storage 启动失败

> 本节的结论**覆盖**上面"关键结论"第 7 条（那条描述的是修复前的状态）。

### 现象
account / storage 启动即挂，`globalTransactionScanner` 初始化失败：
- VPN 开：`can not register RM,err:register TMROLE error, errMsg:null ,cost: 30012 ms` → `can not connect to [198.18.0.1:8091]`
- VPN 关：`connect failed, can not connect to services-server`（10s，`NettyClientBootstrap.getNewChannel`）

### 根因（读字节码 + 实测，非推测）
1. `seata-server/conf/application.yml` 的 `preferred-networks: 192.168.43.*` 已失效
   ——本机 en0 现在是 **192.168.5.3**。历史日志佐证：9/3、9/4 正常时 xid 前缀是 `192.168.43.156:8091`。
2. `NetUtil.getLocalAddress0` 逻辑：跳过回环 → **先记下"第一个合法地址"当兜底** → 有 preferred 就匹配、命中即返回
   → 全不匹配则打 `Could not match ip by preferredNetworks:{} , will use default first ip {} instead.` 并**回退用兜底**。
3. **macOS 上 Java 枚举网卡是按下标倒序的**（实测：utun3(21)→utun2(20)→utun1(19)→utun0(18)→llw0(17)→awdl0(16)→en0(14)→lo0(1)）。
   代理的 `utun4`(index=22, 198.18.0.1) **永远排在 en0 前面** → 兜底选中 198.18.0.1。
4. 排除项：`127.0.0.1` 在扫描阶段就被 `isValidAddress` 过滤（`FORBIDDEN_HOSTS` + `isLoopbackAddress`），
   所以 `preferred-networks: 127.0.0.1` 写不进。

### 关 VPN 为什么没用
Nacos 里那条实例是 TC 注册的**临时实例**，靠 TC 自身心跳续命，Nacos 从不校验 IP 是否真实存在。
关掉 VPN 只是让 utun4 消失，注册记录还在 → 客户端改为直接连不上（10s 失败）。

### 修复（已执行并验证）
`seata-server/conf/application.yml`：`preferred-networks: 192.168.43.*` → **`192.168.*`**
（备份：`conf/application.yml.bak-20260911-002910`；已 kill 旧 TC PID 20289 并用 JDK 17 重启，新 PID 39650）

验证链：
- TC 日志：`Starting ServerApplication v2.6.0 using Java 17.0.12 on 192.168.5.3`
- Nacos：`ip=192.168.5.3 port=8091 healthy=true`（旧的 198.18.0.1 条目已消失）
- account 服务实测：`will connect to 192.168.5.3:8091` → `register TM success 89ms` → `register RM success 7ms`
  → `Started AccountExampleApplication in 5.215 seconds`（Tomcat 8102）**启动成功**

选 `192.168.*` 而不是 `192.168.5.*` 的理由：即使 utun4 排在最前也不匹配，扫描继续到 en0 命中即返回，
**顺序敏感被消除，VPN 开着也能正常注册**。

### 客户端侧（实测结论）
给样例服务加 `-Dseata.registry.preferred-networks=10.99.*` 后，既不打印 `Could not match ip` 警告，
自报 IP 仍是 `192.168.5.3` → **客户端不读 `seata.registry.preferred-networks`**，只走"第一个合法地址"。
客户端自报 IP 仅用于日志，不影响连 TC，无需改客户端。

### 可复用的诊断方法
1. 看 Nacos 注册 IP：`curl -s "http://127.0.0.1:8848/nacos/v1/ns/instance/list?serviceName=seata-server&groupName=SEATA_GROUP"`
2. 对本机网卡：`ipconfig getifaddr en0` / `ifconfig | grep "inet "`
3. 关键判据：**TCP 能握手 ≠ 能通信**。实测 `nc -vz 198.18.0.1 8091` 成功、`curl --max-time 6` 却 `rc=52` 无响应
   ——TUN 只接管握手，数据被吞。
4. 枚举顺序可用纯 JDK 复现：遍历 `NetworkInterface.getNetworkInterfaces()` 打印 `getIndex()` 即可。

### 当前环境状态（2026-09-11 00:36）
Nacos ✅ · TC 8091 ✅（PID 39650，已注册 192.168.5.3）· MySQL ❌ 未启动 · 四个业务服务未常驻
（account 仅为验证临时启动过，已回收）

## 【重要结论】Dubbo 2.7.13 就支持 tag 路由，无需升 3

用户最初以为"tag 是 3.0+ 才有"，**实测证伪**。证据（读 `~/.m2` 里 2.7.13 各 jar + 跑探针）：

1. `dubbo-cluster-2.7.13.jar` 内有 `TagRouter` / `TagRouterFactory`，`TagRouterFactory` 标注
   `@Activate(order=100)` → **默认挂在 RouterChain 上**，不需要额外开启。
2. `AbstractInterfaceConfig` 有 `protected String tag`，`getTag()` 标注
   `@Parameter(key="dubbo.tag", useKeyAsProperty=false)`。
3. `@DubboService` / `@DubboReference`（以及旧的 `@Service` / `@Reference`）都带 `tag()` 属性。
4. `rpc/filter/ContextFilter` 把 `dubbo.tag` / `dubbo.force.tag` 放进 invocation attachment。
5. **实测探针**（用 samples-order fat jar 的 BOOT-INF/lib 做 classpath）：
   `ProviderConfig.setTag("gray")` → URL 参数 `{deprecated=false, dynamic=true, dubbo.tag=gray}`
   `ConsumerConfig.setTag("gray")` → URL 参数 `{sticky=false, dubbo.tag=gray}`
   未设 tag → 无 `dubbo.tag` 参数。**即 `dubbo.provider.tag` / `dubbo.consumer.tag` 直接映射到 `dubbo.tag`。**

### TagRouter.filterUsingStaticTag 的路由语义（读字节码）
- consumer **有** tag=T：先找 provider 中 `dubbo.tag==T` 的
  - 找到 → 只路由到它们（隔离达成）
  - 没找到 → 若 `dubbo.force.tag!=true`（默认 false）→ **回退到"所有无 tag 的 provider"**；
    若 force=true → 返回空 → `No provider available`
- consumer **无** tag：**只保留"无 tag 的 provider"** → 会自动排除所有打标 provider
- ⚠️ **陷阱：必须成对打标**。只给 provider 打 tag、consumer 不打 tag，consumer 会看不到这些 provider，
  直接 `No provider available`（因为无 tag 的 consumer 只认无 tag 的 provider）。
- 严格隔离可加 `dubbo.consumer.parameters."dubbo.force.tag"=true`（TagRouter 的 `url` 是消费端 URL）。

### XID 透传机制（升级评估的关键）
- **不是** Seata 官方模块，而是 **`org.apache.dubbo.extensions:dubbo-filter-seata`**，
  由 **`seata-all:2.6.0` 传递引入**（seata-all 的 pom 里显式声明 1.0.2，排除 seata-core）。
- SPI：`META-INF/dubbo/org.apache.dubbo.rpc.Filter`
  → `seata-provider=org.apache.dubbo.seata.SeataTransactionPropagationProviderFilter`
  → `seata-consumer=org.apache.dubbo.seata.SeataTransactionPropagationConsumerFilter`
- **1.0.2 引用的是 Seata 1.x 的 `io/seata/core/context/RootContext`**，
  而 **Seata 2.6.0 特意保留了 `io.seata` 兼容包**（jar 内 193 个 `io/seata/` 条目），所以能跑通。
- 官方 SPI Extensions 页标注 `dubbo-filter-seata` 适配 **2.7.x ~ 3.x.x**；
  Maven 上还有 3.2.0 / 3.3.0 / 3.3.1 等 Dubbo3 线版本。

### Dubbo 3 升级要求（官方文档，2026-09 检索）
| 项 | 要求 | 本项目现状 | 结论 |
|---|---|---|---|
| Nacos Server | Dubbo 3.x 需 **Nacos 2.x**（3.3.0 推荐 2.2.3，兼容范围 2.x） | Nacos Server 2.4.3 | ✅ 满足 |
| nacos-client | 需 2.x | 2.4.3 | ✅ |
| Spring Boot | dubbo-spring-boot-starter 3.3.x / 3.2.x 兼容 **1.x ~ 3.x** | 2.2.2.RELEASE | ✅ 在范围内 |
| JDK | 3.0.x→8；3.2.x→8/17；3.3.x→8/17/21 | 服务用 8；TC 用 17 | ✅ |
| 默认注册模式 | Dubbo 3 默认 `register-mode=all`（接口级+应用级双写），消费端默认双订阅 | 2.7.13 仅接口级 | ⚠️ Nacos 里会多出应用级注册 |
| 配置清理 | `com.alibaba.spring:spring-context-support`（Dubbo2 遗留）、`dubbo.application.id` 等在 3.x 已不必要 | 项目里都有 | ⚠️ 需清理 |
| Seata 事务 | XID 靠 `dubbo-filter-seata`，官方称兼容 3.x，但需换对应版本并回归验证 | 1.0.2（Dubbo2 线） | ⚠️ 需实测回归 buy/buy2 |
| 顺带 | business / order 的 `dubbo.consumer.provided-by` 在 2.7.13 是死配置 | — | 可删 |

**结论：为实现 tag 隔离，留在 2.7.13 配 tag 成本最低（零升级风险）；升 Dubbo 3 是独立决策，不是 tag 的前提。**

## 新建 `documents/问题记录.md`

问题记录台账文档，结构：文档说明 → 索引表（编号/日期/标题/影响组件/状态）→ 逐条问题详情 → 附录 A 新增问题模板。
首条收录的就是上面这个 **问题 #001：TC 注册到虚拟网卡 198.18.0.1**，
按「基本信息 / 现象 / 根因 / 关VPN无效的原因 / 修复 / 验证 / 排除的三种无效写法 / 可复用诊断方法」组织。
约定：新问题编号顺延（#002…）并在索引表登记一行；只记实证结论，不写推测。

---

## Dubbo 2.7.13 → 3.3.6 升级（已完成并端到端复测）

用户要求：真实项目都用 Dubbo 3.x，把本学习工程也升到 3。

### 改动清单
- 根 `pom.xml`：`dubbo.version` 2.7.13 → **3.3.6**；`netty.version` 4.1.42.Final → **4.1.101.Final**；新增 `dubbo-filter-seata.version=3.3.1`。
- 依赖收敛：`dubbo-spring-boot-starter` 为唯一 starter（其传递的 all-in-one `dubbo` 聚合包已含 `dubbo-config-spring`/`dubbo-cluster`/`dubbo-configcenter-nacos` 等）；
  **移除**单独声明的 `dubbo`、`dubbo-config-spring` 与 `com.alibaba.spring:spring-context-support:1.0.11`；显式加 `dubbo-registry-nacos` + `org.apache.dubbo.extensions:dubbo-filter-seata:3.3.1`。
- 3 个 `*DubboServiceImpl`：`@DubboService` 去掉 Dubbo3 已废弃的 `protocol/application/registry` 属性；删无用 import。
- 4 个 `application.yml`：`dubbo.consumer.parameters` 由方括号改**点号**；business/order 删 `provided-by`。
- 改动前备份：`.workbuddy/backup-dubbo3/`。

### 三个关键踩坑（已在 问题记录.md #002 + 手册 §9 详录）
1. **netty 胖包**：`netty-all` 自 4.1.91 起是"空壳聚合器"（0 类），而 4.1.42 是 2667 类的胖包，会与子模块重复类。
   取 Dubbo3.3.6(声明 4.2.2) 与 Seata2.6.0(声明 4.1.101) 交集 → 4.1.101；已核实 Dubbo 3.3.6 不用任何 netty 4.2 独有 API。**不可回退到 4.1.91 以下**。
2. **`dubbo.force.tag` 方括号写法不生效**：`"[dubbo.force.tag]": "true"` 绑不进去（`parameters` 定义在 `AbstractMethodConfig` 上，Dubbo 绑定器不展开括号 key），
   必须写点号 `dubbo.force.tag: "true"`；验证看**消费端 URL** 是否出现该参数（只看 yml 会被骗）。
3. **启动失败晚 30 秒**：Dubbo 3 把消费端校验挪到 `onContextRefreshedEvent`（Spring refresh **之后**）
   → `DefaultModuleDeployer.checkReferences()` → `SimpleReferenceCache.check` → `ReferenceConfig.checkInvokerAvailable(timeout)`，
   **每 100ms 轮询、默认 30000ms** 超时后才抛 `IllegalStateException`。
   超时项 `dubbo.module.check-reference-timeout`（毫秒，默认 30000；设 0 = 立即失败）。
   ⚠️ 中途会打印 `Referred dubbo service` / `Migrated to APPLICATION_FIRST mode` / `Dubbo Application … is ready`，**别当成启动成功**。
   → **结论：启动顺序仍是硬性的，只是失败延后 30 秒**（我第一遍被那几行 `is ready` 骗了，以为 Dubbo3 不再 fail-fast；重做受控实验等满 30s 才复现异常）。
   另：Dubbo 3 标签路由类名由 `TagRouter/TagRouterFactory` → `TagStateRouter/TagStateRouterFactory`（仍 `@Activate(order=100)` 默认启用），tag 方案原样可用。

### 复测结果
- JDK 8 `mvn clean package -DskipTests` → BUILD SUCCESS；4 服务全部启动，日志 Dubbo 版本 `3.3.6`。
- `buy` 提交：account 3200→3100、storage 500→450、t_order +1、undo_log 清空。
- `buy2` 回滚：数据不变、undo_log 清空。
- XID 同一串（`192.168.5.3:8091:…`）在 4 服务日志出现 → Seata 经 `dubbo-filter-seata` 透传 OK。
- tag 隔离：11 次调用全命中 `dev`；`other` 实例 0 次；`force.tag` 下异 tag 抛 `No provider available for StorageDubboService`。

### 文档更新
- `documents/本地编译启动手册-实测跑通.md`：适用范围改 Dubbo 3.3.6；§5 标题 + §5.3 重写（30s 延迟失败 + 反编译调用链 + 配置项）；
  §5.4 `provided-by` 说明更新；§7 踩坑清单加 7/8/9 三行；**新增 §9「Dubbo 3 升级要点」**。
- `documents/问题记录.md`：索引加 #002；新增「问题 #002：Dubbo 2.7.13 → 3.3.6 升级过程中的三个坑」。

### 环境留痕（升级验证后）
- TC `192.168.5.3:8091`（pid 39650，JDK17）；Nacos 8848（pid 19542）；4 服务 8101/8102/8104/8109（均 404 = Tomcat 存活）。
  验证期间 account 重启过一次（pid 77317 → 82806）。
- 复用脚本：`/tmp/svc.py <account|order|storage|business> [serverPort] [extra java args]`（`start_new_session=True` 防被回收）。

---

## 08:50 事故排查：本机 IP 漂移导致全量注册失效（问题 #003）

用户报"升级 dubbo3 后启动失败"，贴了 storage 启动栈：`can not connect to [192.168.5.3:8091]` / `Caused by: connect failed, can not connect to services-server`。

**判定：与 Dubbo 3 无关**——异常抛在 `GlobalTransactionScanner.afterPropertiesSet`（Seata 阶段，早于 Dubbo）。

**根因**：本机 `en0` 已从昨晚的 `192.168.5.3` 变为 **`192.168.43.156`**，但 Nacos 里的注册 IP 是上次启动时写死的旧值：
- `seata-server` 注册 = `192.168.5.3:8091`（`nc -z` 该地址失败）
- 4 个服务的 Dubbo 注册（接口级 + 应用级）**全部** = `192.168.5.3:…`
- `lsof -iTCP:8091` 显示 TC 仍正常监听 `*:8091` —— **"TC 在跑" ≠ "注册 IP 对"**

**修复**：`preferred-networks: 192.168.*` 已能覆盖新 IP，**无需改配置**；只需 kill 旧 TC + 4 服务 → 重启。
另发现 `target/*.jar` 不存在（只剩 `classes/`），先用 JDK 8 `mvn clean package -DskipTests` 重新打包（BUILD SUCCESS, 7.4s）。

**验证**（全过）：
- TC 注册 → `192.168.43.156:8091`（旧条目心跳超时后自动剔除）
- 4 服务注册 → `192.168.43.156:20883/20880/20888`，全部 `Started *ExampleApplication`
- `buy2` 回滚：HTTP 500、数据不变、TC `Rollback global transaction successfully`
- `buy` 提交：HTTP 200、storage 450→400、account 3100→3000、orders 9→10、`undo_log` 归零、
  TC 3× `PhaseTwo_Committed` + `Committing global transaction is successfully done`
- XID 同一串 `192.168.43.156:8091:964592193717702661` 出现在 4 个服务日志

**当前进程**：TC pid 67349；account 68059 / storage 68061 / order 68292 / business 68492。

**文档**：`问题记录.md` 加 #003（含"换网络后一行自检"命令）；手册 §4 顶部加"每次开机/换网后必做自检"警示块，§7 踩坑清单加第 10 行。

### 关键教训（易误判）
- 报错 IP 是"本机曾经用过的网段"（且不像 198.18.x 那样一眼假）时，**先查 Nacos 注册 IP vs 本机 IP**，别怀疑代码或版本升级。
- 换网络后要重启的是 **TC + 所有已运行服务**，只重启 TC 会让 Dubbo 侧继续指向旧 IP。

### 概念澄清：yml 里的 127.0.0.1 ≠ Nacos 里注册的 IP（用户提问引出）
用户质疑"全是本机服务、配置写的都是 127.0.0.1，为何还受 IP 变化影响"。结论（已写进手册 §4.1 顶部警示块）：
- **两个不同语义的 IP**：yml 的 `nacos://127.0.0.1:8848` 是"去哪注册"（Nacos 服务端地址，客户端连出去）；
  Nacos 实例记录里的 IP 是"注册成什么"（自报家门地址，**配置里根本没写**，框架启动时探测网卡得来）。
- 消费者**不用自己的 127.0.0.1 去连提供者**，而是从 Nacos 取回提供者自报的地址再连 → 所以同机也走真实网卡 IP。
- 框架**在网卡扫描阶段就剔除回环**（字节码证据）：Seata `NetUtil.isValidAddress` 首行 `isLoopbackAddress()` → return false；
  Dubbo `NetUtils` 把 `127.` / `127.0.0.1` / `0.0.0.0` / `localhost` 列入 invalid local host，遍历网卡时 `NetworkInterface.isLoopback()` 跳过。
- **不能靠"强制 127.0.0.1"一劳永逸**：Dubbo 侧有 `dubbo.protocol.host`（`ProtocolConfig` 真实字段）可强指；
  但 Seata 侧不行——`seata-all-2.6.0` 的 `NetUtil` 全类搜不到 `register-ip` / `host` 直配项，只有 `preferred-networks`，
  且回环在匹配前就被剔除，`preferred-networks: 127.*` 永远匹配不上。两边改不齐 → 仍会被 IP 漂移打断，故选择"不强制、换网后重启"。

### 概念澄清 2：启动失败的元凶只有 `seata-server` 那一条注册（用户第二次质疑引出）
用户追问"启动失败和 Nacos 注册的 IP 没关系吧，还没发起 Dubbo 调用呢"。**一半成立**，已写进 `问题记录.md` #003 新增的「三、易混淆点」：
- **两条注册、两个读取时机**：`SEATA_GROUP` 的 `seata-server` 在**启动时**被 Seata 客户端读（`GlobalTransactionScanner.afterPropertiesSet`
  → `NacosRegistryServiceImpl` 查命名服务）→ 错则**启动即失败**；`DEFAULT_GROUP` 的 `providers:*` 在**调用时**才被 Dubbo 消费者解析
  → 错则**启动正常、调用报 `No provider available`**。后者与本次启动失败**无关**（进程没走到那步就死了）。
- 实测时序（account 日志行号）：行81 `NacosRegistryServiceImpl` 08:52:48.430 → 行85 `will connect to 192.168.43.156:8091` 08:52:48.515
  → 行94/133 `register TM/RM success` → 行154 `[DUBBO] Subscribe` 08:52:51.351。**Seata 连 TC 比 Dubbo 首次订阅早约 3 秒**。
- 断言依据：`192.168.5.3` 在项目 4 个 yml 与 TC 的 `conf/` 里 **grep 一处都不存在**；且配置里连 `8091` 端口都没有。
  重启后未改任何配置即自动连到 `192.168.43.156:8091` → 地址只可能来自 Nacos 注册表。
- 顺带修正了 #003「基本信息/根因」两处的措辞（原先把 4 个 Dubbo 注册并列成"影响范围"，易被误读为元凶），并给「六、可复用经验」加了
  「按报错时机区分两条注册」的判据表。子章节编号已顺延（三=易混淆点，四=修复，五=验证，六=可复用经验）。

### 重启范围实测（用户问"VPN 导致 TC IP 变要不要重启"引出）
**结论：TC 必须重启；业务服务不用（仅 Seata 而言）。** 已写进 `问题记录.md` #003 新增「六、重启范围实测」+ 手册 §4.1「重启范围速查」表。
- **TC 无热更新**：注册 IP 由 `NetUtil` 启动时算一次；`seata-all-2.6.0` 的 `NetUtil` 无 `register-ip`/`host` 直配项，也无定时重注册任务 → IP 变只能重启。
- **客户端能自动跟随**（反编译 + 实测双向验证）：`doReconnect` → `getAvailServerList` → `RegistryService.lookup(group)`；
  周期 `AbstractNettyRemotingClient.init()` → `scheduleAtFixedRate(runnable, 60000L, 10000L, MILLISECONDS)`（首延迟 60s，之后每 10s）。
- **实测**（4 服务 PID 全程 68059/68292/68061/68492 未变）：停 TC + 手工塞假实例 `127.0.0.1:18091` → 4 服务 ≤6s 内全部
  `will connect to 127.0.0.1:18091`；删假实例 + 重启 TC → 又全部跟回 `192.168.43.156:8091`。`buy2` 全程正常回滚，数据未变。
- **优雅关闭会注销**：`SIGTERM` 停 TC 后 Nacos 里 `seata-server` 立刻变 `"hosts":[]`；只有 `kill -9` 才残留死地址。
- **VPN 场景的真相**：开关 VPN 通常**不用重启** —— TUN 网卡是 198.18.x / 10.x，`preferred-networks: 192.168.*` 匹配不上会被跳过。
  真正需要重启的是**换 Wi-Fi / 换网段**；而且那时业务服务要重启是**为了修它们自己的 Dubbo 注册**，不是为 Seata。
- 注入假实例的坑：目标服务已是临时实例时，POST 注册"持久化实例"会被拒
  （`errCode: 400 ... is ephemeral service, can't register persistent instance`），必须 `ephemeral=true` + 循环重注册当心跳保活。
- 新建 skill `~/.workbuddy/skills/nacos-seata-registry-ip-debug/`（两条注册区分 / 自检命令 / 重启矩阵 / 假实例验证手法）。
- 环境已恢复：TC pid 89532（`192.168.43.156:8091`）、Nacos 19542、4 服务 68292/68059/68492/68061。

### 10:0x 再起 account 报 `BindException: Address already in use` —— 又一次假故障
- 现象：用户重启 TC 后启动 account，报 `Failed to bind NettyServer on /0.0.0.0:20883, cause: Address already in use`。
- 根因：**20883 被"已在运行的 account 实例"（pid 68059）占着** —— 不是 Seata、不是 IP 漂移。起第二个实例必然撞端口。
- 佐证：`lsof -nP -iTCP:20883` → 68059；本机 IP 仍是 `192.168.43.156` 未变；Nacos 里 account 注册 `healthy=true`；
  68059 日志在 10:01:02 已 `register TM/RM success`（说明 TC 重启后它已自动重连，**根本不需要重启业务服务**）。
- 另：`curl` 报 `HTTP=000` 是本机 `http_proxy=http://127.0.0.1:54220` 抖了一下，重试即 500（buy2 正常回滚）。
- 文档：手册 §7 踩坑清单加第 11 行；skill `nacos-seata-registry-ip-debug` 加"别把端口被占误判成 IP 问题"及 description 补关键词。
- 用户选择：**停掉我后台起的 4 个实例**，改由他自己从 IDE 启动。
  已按 SIGTERM 优雅停止（68059/68061/68292/68492，3s 内端口全释放），Nacos 里 provider 接口级与应用级注册**均已自动摘除**。
  现状：**TC 91779（192.168.43.156:8091）+ Nacos 19542 保留**；8101/8102/8104/8109/20880/20883/20888 全部空闲。
  注意：zsh 不做变量分词，`for p in $PIDS` 会把整串当一个参数（报 `illegal pid`），要么用 `${=PIDS}` 要么直接逐个写。

### 10:1x 用户自启 TC 踩了两个坑（已修复 + 已归档为 #004 / #005）

**#004 TC 跑在 JDK 8 → 客户端 RPC timeout 30s**
- 用户 `sh bin/seata-server.sh` 重启 TC，脚本按环境变量 `JAVA_HOME`（指向 jdk-1.8）选了 JDK 8。
- 现象极具迷惑性：客户端 `TmTransactionException: RPC timeout` / `GlobalBeginRequest ... cost: 30009 ms`（挂满 30s），
  而 **TC 侧三项表象全正常**（进程在、8091 LISTEN、Nacos healthy=true、启动日志正常）。
  真相只在 `~/logs/seata/seata-server.8091.error.log`：
  `NoSuchMethodError: java.nio.ByteBuffer.clear()Ljava/nio/ByteBuffer;` at `GlobalSession.encode`（TC 吞掉异常不回包）。
- **修复（根治）**：改造 `.../seata-2.6.0/seata-server/bin/seata-server.sh`（原脚本备份 `.orig`），
  在 `. ${BASEDIR}/bin/seata-setup.sh` **之前** 注入 `SEATA_JAVA_HOME`（jdk-17，兜底 `/usr/libexec/java_home -v 17`）并 `export JAVA_HOME/JAVACMD`。
  **必须在 source 之前** —— `seata-setup.sh` 按 `JAVACMD` 版本生成 GC 参数（JDK9+ → `-Xlog:gc*:file=`；JDK8 → `-Xloggc:`），放在之后会「JDK17 跑 + JDK8 GC 参数」错配。
  验证：启动行出现 `[INFO] 使用 JDK: .../jdk-17.jdk/...` 且 GC 参数变成 `-Xlog:gc*`；TC 日志 `using Java 17.0.12`；`GlobalBeginRequest` 16ms 回包；`NoSuchMethodError` 计数 0。
- 教训：**"TC 在监听 + Nacos healthy" ≠ "TC 可用"**；排查 RPC timeout 必须同时看 TC 侧 error 日志。

**#005 服务注册凭空消失 + HTTP 000 → 实为 IDE 调试断点冻结整个 JVM**
- 现象：`No provider available ... StorageDubboService` 且 `invokers: 0[]`；Nacos 里接口级与应用级注册**同时为空**；
  但 storage 进程在、8109/20888 仍 LISTEN；`curl 127.0.0.1:8109/` 返回 **`000`**；另有一次 buy2 卡 53s 报 `Channel ... is inactive`。
- 根因：storage 在 **Debug 模式**下，`TStorageServiceImpl.decreaseStorage(TStorageServiceImpl.java:24)` 上留着断点；
  IDEA 默认 suspend=All，命中即冻结整个 JVM → Nacos 心跳停发（约 15s 摘除）+ Tomcat 不响应 + Dubbo 连接 inactive。
- 铁证 `jstack <pid>`：`"DubboServerHandler-...-thread-7" ... **at breakpoint**`，栈顶正是 `TStorageServiceImpl.java:24`。
  ⚠️ `Thread.State` 仍显示 **RUNNABLE**、无死锁 —— 只有行尾的 `at breakpoint` 才是标志。
- 修复：IDE 里取消/放行断点。放行后 `HTTP=404`、注册恢复 healthy、buy2 0.028s 正常回滚。
- 判据（可复用）：**进程在 + 端口 LISTEN + 应用不响应(000) + 注册凭空消失 → `jstack <pid> | grep -n "at breakpoint"`**。
  另：`HTTP=404` = 应用活着；`HTTP=000` = 应用不响应。

**文档/产物**：手册 4.2 增「已改造脚本 + 手工命令 + 为什么必须放在 source 之前」；踩坑清单更新第 2 行、新增第 12 行；
问题记录 新增 **#004 / #005**（含索引登记），并在 #003「七、可复用经验」后加了指向 #005 的辨析；
skill `nacos-seata-registry-ip-debug` 补了两条防误判并更新 description。
**环境**：TC pid 98869（jdk-17，`192.168.43.156:8091` healthy）；4 个 IDE 服务 94831/94915/94849/94997；
⚠️ 用户 IDE 里 storage 的断点仍需自行取消，否则 `buy` 仍会失败。
